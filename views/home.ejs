<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Restaurants</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    </head>
    <body>
        <div class="jumbotron text-center">
            <h1>Hello! user: <%= name %></h1>
           <p>Showing all <%= nRestaurant %> documents.</p>
        </div>
        <div class="container-fluid">
            <div class="row ml-md-auto">
                <a href="/logout" class="btn btn-primary pull-right" role="button">Log out :)</a>
                <a href="/create" class="btn btn-primary" role="button">Create new restaurant</a>
            </div>
            <br>
            <div class="row">
                <p>Search restaurant name/borough/cuisine: (Try search: Name:'Old Fung Tea House', borough: 'Yuen Long', cuisine: 'Dim sum')
                <label for="bar"><span class="glyphicon glyphicon-search"></span></label><input style="width:100%" type="text" id="myInput" onkeyup="myFunction()" placeholder="Search name/borough/cuisine:" name="bar"><br></p>
                <table id="myTable" class="table table-striped table-bordered table-hover table-scroll">
                    <thead ><tr class="table-primary"><th>Restaurant Name</th></tr></thead>
                    <tbody style='height:300px;display:block;overflow:scroll'>
                        <% for (var r of restaurant) {%>
                            <tr>
                                <td><a href="/details?_id=<%= r._id %>"><%= r.name %></a></td>
                                <td style="display:none;"><%= r.borough%></td>
                                <td style="display:none;"><%= r.cuisine%></td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
            <p>This is the end of the page ;)</p>
        </div>
        <script>
        $(document).ready(function(){
            $("#myInput").on("keyup", function() {
                var value = $(this).val().toLowerCase();
                $("#myTable tr").filter(function() {
                    $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
                });
            });
        });

        </script>
    </body>
</html>